// Archived advanced columnar demo example (not built by default).
// This example referenced experimental types and async page manager traits
// that are not currently exported. Use `examples/olap_kv_scan.rs` instead.

fn main() {
    eprintln!("This archived example is not compiled by default. See examples/olap_kv_scan.rs for analytics KV scans.");
}

